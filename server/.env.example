
# Server Configuration
PORT=5000
NODE_ENV=development

# Chat Encryption Key - Keep this secure and never commit to version control!
# Generate a secure key using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CHAT_ENCRYPTION_KEY=your-secure-encryption-key-here-change-this-in-production
